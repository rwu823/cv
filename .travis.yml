language: node_js

node_js:
  - v7

env:
  matrix:
    - TRAVIS_MATRIX=build

script:
  - ls -al
  - npm run $TRAVIS_MATRIX

after_success:
  - node scripts/deploy

branches:
  except:
    - /^(v?\d+\.\d+\.\d+)/
